{
  "task_id": "task-correct-context-packet-measurement",
  "parent_decision_id": "decision-a4562df8-d652-4698-b529-d8921a62a707",
  "title": "Correct Context Firewall packet measurement stability",
  "objective": "Fix the demonstrated self-referential compact-byte measurement failure without mutating historical packets, and add adversarial deterministic coverage.",
  "scope": [
    "Context Firewall packet byte accounting in src/runner.js",
    "directly relevant tests only"
  ],
  "authorization": {
    "may": [
      "inspect current Runner and packet tests",
      "modify packet measurement implementation",
      "add focused adversarial tests",
      "run the full deterministic suite"
    ],
    "may_modify": [
      "src/runner.js",
      "tests/**"
    ],
    "may_not": [
      "modify historical .opsle packets, evidence, policy, objective, or requirements",
      "modify unrelated source or documentation",
      "invoke Claude or another provider",
      "weaken packet completeness or durable-size integrity checks",
      "deploy, merge, push, or create a PR"
    ]
  },
  "required_inputs": [
    "src/runner.js",
    "tests/operator-controls.test.js",
    ".opsle/evidence/compact/task-final-reconcile-requirements-attempt-001.failure.json",
    ".opsle/evidence/raw/task-final-reconcile-requirements-attempt-001/execution.json"
  ],
  "relevant_context": [
    "Attempt 1 execution and verification both exited 0, then contextPacket exhausted its 20-iteration fixed-point loop",
    "Historical packets are immutable and use the recorded fields truthfully",
    "A corrected schema/measurement basis must be explicit if semantics change"
  ],
  "expected_deliverable": "A bounded fix that cannot oscillate on self-referential byte fields, with a regression test reproducing the boundary class.",
  "expected_evidence": [
    "regression test fails against the old bounded-iteration algorithm or explicitly covers a no-fixed-point boundary",
    "durable packet byte measurement has deterministic documented semantics",
    "written packet size and reported metrics remain internally consistent",
    "historical packets are not rewritten",
    "npm run verify exits 0",
    "actual changed files remain authorized"
  ],
  "acceptance_criteria": [
    "no bounded fixed-point nonconvergence remains",
    "the prior size-boundary failure class has deterministic coverage",
    "packet completeness is not weakened",
    "npm run verify exits 0",
    "no unexpected files change",
    "return packet is complete_for_decision"
  ],
  "prohibited_actions": [
    "deleting the compact-byte check without replacement",
    "rewriting historical evidence",
    "broad refactoring",
    "provider invocation from tests"
  ],
  "operator_policy_constraints": [
    "Codex enabled",
    "Claude disabled",
    "review off",
    "model polling prohibited"
  ],
  "requirement_ids": [],
  "route_hint": "codex",
  "deterministic_command": null,
  "verification_command": [
    "npm",
    "run",
    "verify"
  ],
  "expects_changes": true,
  "timeout_seconds": 1800
}
