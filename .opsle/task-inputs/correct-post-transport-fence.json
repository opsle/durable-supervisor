{
  "task_id": "task-correct-post-transport-fence",
  "title": "Revalidate delivery fence after resume",
  "objective": "Correct the stale-delivery race found by task-final-live-resume-proof. After the bounded plain codex resume transport returns with confirmation and cleanup, revalidate the queue version, supervisor identity/generation, dispatcher process/generation, and activation lease/fencing token immediately before any DELIVERED decision, receipt commit, state activation, telemetry, or delivery event mutation. A mid-transport recovery, dispatcher replacement, request change, or lease takeover must fail closed without a DELIVERED receipt.",
  "scope": [
    "src/wakeup.js post-transport delivery fence",
    "tests/wakeup.test.js mid-transport generation, dispatcher, queue, and lease-change regressions",
    "minimal operations documentation if needed"
  ],
  "required_inputs": [
    "src/wakeup.js",
    "tests/wakeup.test.js",
    ".opsle/evidence/compact/task-final-live-resume-proof-attempt-001.json"
  ],
  "relevant_context": [
    "Pre-transport validation occurs before a transport that may remain active for up to 120 seconds plus cleanup.",
    "Current code commits DELIVERED after return without re-running validateDeliveryFence and decisionFenceCurrent.",
    "A stale transport may have delivered a message, so fence loss after transport is UNCERTAIN and must not replay or commit a delivered receipt.",
    "Canonical argv, exact rollout evidence, temporary process cleanup, Herdr continuity, busy behavior, and pause semantics remain fixed."
  ],
  "acceptance_criteria": [
    "all delivery fences are revalidated after transport and before DELIVERED mutation",
    "post-transport generation, dispatcher, queue, request, or lease drift fails closed",
    "no DELIVERED receipt, state activation, activation telemetry, or HOST_WAKE_DELIVERED event is emitted after fence loss",
    "the uncertain decision is non-replayable",
    "normal unchanged-fence delivery still succeeds exactly once",
    "focused and full verification pass",
    "Context Firewall packet is complete_for_decision"
  ],
  "expected_deliverable": "A minimal post-transport fence correction with deterministic race regressions.",
  "expected_evidence": [
    "source diff",
    "mid-transport fence-change tests",
    "unchanged-fence positive test",
    "focused and full test output",
    "git diff --check",
    "complete Context Firewall packet"
  ],
  "authorization": {
    "may": [
      "read repository and named compact evidence",
      "modify src/wakeup.js, directly related tests, and minimal docs",
      "run focused and full local verification"
    ],
    "may_modify": [
      "src/wakeup.js",
      "tests/wakeup.test.js",
      "docs/OPERATIONS.md"
    ],
    "may_not": [
      "invoke live wake, signal live runtime, replay or rewrite historical events",
      "change canonical message/argv, timeout, policy, objective, identity, binding, pause behavior, or host configuration",
      "modify siblings, push, merge, deploy, install, or initialize a repository"
    ]
  },
  "prohibited_actions": [
    "live wake",
    "uncertain-event replay",
    "terminal input",
    "model polling",
    "release or deployment"
  ],
  "operator_policy_constraints": [
    "Codex enabled",
    "Claude disabled",
    "review off",
    "Gearbox required",
    "Context Firewall required",
    "model polling prohibited"
  ],
  "route_hint": "codex",
  "expects_changes": true,
  "timeout_seconds": 900,
  "verification_command": [
    "node",
    "--test",
    "--test-isolation=none",
    "tests/wakeup.test.js"
  ],
  "requirement_ids": []
}
